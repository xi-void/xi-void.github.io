# The app uses no reflection, serialization, or dynamic class loading, so the
# defaults from proguard-android-optimize.txt are sufficient. R8 is free to
# strip and inline aggressively. Nothing to keep.
